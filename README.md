# Install dependencies
Firstly, install python and don't forget to enable adding to PATH. Then you can go to your directory and run in cmd/bash:
```bash
pip install -r requirements.txt
```
# Run it
```bash
python main.py
```
